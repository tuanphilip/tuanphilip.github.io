# Upwork Profile & Pitching Kit

## 1. Main Profile (AI & Python Systems)

* **Title:** `AI Systems & Python Engineer | LLM / RAG Pipelines • Scraping • Production MVPs`
* **Hourly Rate:** `$35.00/hr`

### Overview (Copy-paste ready):
```markdown
Most MVP launches and data pipelines fail because they are built on fragile, unmaintainable boilerplate that collapses under real production load.

I build production-grade AI systems, high-throughput data scrapers, and scalable full-stack backends that founders can rely on from Day 1.

⚡ WHAT I DELIVER:

1. AI & LLM Automation Pipelines:
- Retrieval-Augmented Generation (RAG) over complex documents (PDFs, research papers, legal text) with grounded citation & zero hallucination guards.
- Multi-agent workflows, prompt engineering, and structured JSON output extraction.
- Local & cloud model integrations (OpenAI, Claude, vLLM, Ollama, DeepSeek).

2. Enterprise Web Scraping & Data Pipelines:
- High-volume crawlers bypassing Cloudflare/anti-bot protections using Playwright/Scrapy.
- Async workers (Celery/Redis), data normalization, and continuous sync to PostgreSQL/Cloudflare R2/Supabase.

3. Rapid Production MVPs (7–14 Days):
- Modern backend architectures using FastAPI/Python + Next.js App Router frontend.
- Clean containerization (Docker, Coolify, VPS deployment) with CI/CD and secure auth.

🛠 CORE STACK:
Python (FastAPI, PyMuPDF, Scrapy, Playwright), TypeScript, Next.js, PostgreSQL, Redis, Docker, Coolify, Linux VPS.

📂 PROVEN TRACK RECORD:
- Scientific Paper RAG & Extraction Pipeline (Sub-second extraction, 99.2% accuracy).
- Multi-channel AI CX & Automation Platform (Real-time webhook routing & async analytics).
- Headless digital commerce & LMS systems serving thousands of active learners.

Let's cut through the fluff and build something that actually works. Available for a quick 10-minute architecture alignment call today.
```

---

## 2. Specialized Profile (Enterprise WordPress & Headless CMS)

* **Title:** `Headless WordPress & Full-Stack Engineer | Next.js • Custom Plugins • High Performance`
* **Hourly Rate:** `$35.00/hr`

### Overview:
```markdown
Need enterprise-grade performance, custom functionality, or a headless frontend for your WordPress system?

I specialize in building custom, high-speed WordPress architectures that look and perform like modern SaaS products.

⚡ SPECIALIZED EXPERTISE:
- Headless WordPress: Decoupled WordPress backend via REST/GraphQL paired with Next.js / Tailwind frontend for sub-second page loads.
- Custom Plugin Development: Custom payment gateways, webhook sync, LMS integrations (TutorLMS/WooCommerce), and automation scripts.
- Database & Performance Tuning: Advanced MariaDB/MySQL indexing, Redis Object Cache, Core Web Vitals 95+ optimization.
- Migration & Security: Safe multi-site migration, malware cleanup, and strict server hardening on Linux VPS/Docker.

Let's discuss your project requirements and optimize your platform.
```

---

## 3. Battle-Tested Proposal Templates

### Template 1: AI / RAG / Data Extraction Jobs
```markdown
Hi,

I reviewed your requirements for [specific problem, e.g., extracting structured JSON from complex documents / building an automated AI workflow]. 

Here is how I would structure this pipeline cleanly:
1. Extraction & Parsing: Use [e.g., PyMuPDF / async parser] to extract structured tokens without layout loss.
2. Normalization & LLM Gate: Run through a schema-enforced pipeline [Pydantic + Claude/OpenAI] with strict validation to prevent hallucinations.
3. Storage & Delivery: Stream results directly into your [PostgreSQL / API / Webhook].

I recently built a similar extraction architecture (SCIDEX) that handles multi-page papers with 99%+ accuracy. Here is the architecture overview and live demo: [Link to Portfolio Case Study]

Are you free for a quick 5-10 minute chat today to align on the technical specs and delivery timeline?

Best,
Tuan Philip
```

### Template 2: Web Scraping & Data Pipeline Jobs
```markdown
Hi,

Scraping [Target Website/Platform] reliably comes down to two bottlenecks: anti-bot rate limiting and data consistency.

I build resilient, distributed scraping pipelines using async Python (Playwright / Scrapy / Curl-cffi) with automatic proxy rotation and exponential backoff retry mechanisms. The data is validated through Pydantic before landing in your database, so you never get corrupted records.

Recent benchmark: Built a distributed pipeline harvesting 50k+ daily records with <0.1% failure rate.

I can deliver a working proof-of-concept for your target site within 24–48 hours. What specific fields and export format (PostgreSQL, CSV, JSON, S3) do you need?

Best,
Tuan Philip
```
